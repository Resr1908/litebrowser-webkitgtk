Place compiled WebKit content filters here.

If you compile an Adblock list into a WebKit filter store named `adblock`,
the app will load it on startup:

- Store directory: `resources/filters/store/` (created by your compiler tool)
- Filter name: `adblock`
